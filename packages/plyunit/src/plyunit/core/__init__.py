"""Engine core: app, logging, types, components, and units.

The engine's pure domain layer (types, components, units, and interfaces),
which does not depend on the renderer backend. Contains ``App``,
``configure_logging``, and ``UnitRegistry``.
"""
